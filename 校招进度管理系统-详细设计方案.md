# 校招进度管理系统详细设计方案

> 文档状态：V1.0（开发基线）  
> 项目形态：前后端分离  
> 前端：Vue 3 + TypeScript  
> 后端：Python + Django + Django REST Framework  
> 数据库：PostgreSQL  
> 开发方式：TDD（测试驱动开发）

---

## 1. 项目目标

建设一个面向校招生的个人秋招进度管理系统。用户注册并登录后，只能维护自己的公司投递记录；系统支持公司进度的新增、删除、查询、修改，以及分页、每页数量调整和关键字筛选。

系统需要满足以下核心目标：

1. 用户数据和投递数据均持久化到 PostgreSQL。
2. 登录态基于 JWT Token，浏览器端登录有效期为 7 天。
3. Access Token 失效后，前端能够自动刷新并重试原请求。
4. 每个用户只能访问自己的投递记录，禁止跨用户读取或修改。
5. 公司投递记录支持 AI 面、笔试、一面、二面、三面、HR 面时间管理，所有阶段时间默认为空。
6. 列表查询支持分页、调整每页数量、搜索、筛选和排序。
7. 使用 TDD 模式实现，先写失败测试，再实现功能，最后重构。

---

## 2. 项目范围

### 2.1 本期包含

- 用户注册。
- 用户登录。
- 用户退出。
- Access Token 自动刷新。
- 获取当前登录用户信息。
- 公司投递记录新增、查询、详情、修改、删除。
- 面试阶段时间填写、选择、清空。
- 分页与每页数量调整。
- 按公司名、岗位名搜索。
- 按当前进度、投递状态筛选。
- 按创建时间、更新时间、投递时间、阶段时间排序。
- 前后端单元测试、接口测试和核心流程端到端测试。

### 2.2 本期不包含

- 第三方账号登录。
- 邮箱验证码和找回密码。
- 简历文件上传。
- 多人协作或记录共享。
- 消息提醒、日历同步。
- 数据统计大屏。
- 移动端原生应用。

上述功能可作为后续版本扩展，不进入当前版本验收范围。

---

## 3. 总体架构

```text
┌─────────────────────────────┐
│ Vue 3 单页应用               │
│ Router / Pinia / Axios       │
└──────────────┬──────────────┘
               │ HTTPS + JSON REST API
               │ Authorization: Bearer <access_token>
┌──────────────▼──────────────┐
│ Django REST Framework        │
│ 认证 / 权限 / 业务 / 分页     │
└──────────────┬──────────────┘
               │ Django ORM
┌──────────────▼──────────────┐
│ PostgreSQL                   │
│ 用户 / 投递记录 / Token 黑名单 │
└─────────────────────────────┘
```

### 3.1 架构原则

- 前端只通过 API 操作数据，不直接接触数据库。
- 后端是权限和数据合法性的最终裁决者，不能只依赖前端校验。
- 所有投递记录查询都必须自动限定为当前用户。
- API 统一使用 JSON；日期时间统一使用 ISO 8601 格式。
- 数据库存储 UTC，接口返回带时区的时间，前端按浏览器本地时区展示。
- API 版本统一使用 `/api/v1/` 前缀，为后续升级保留空间。

---

## 4. 推荐技术栈

### 4.1 前端

| 类型 | 技术 | 用途 |
|---|---|---|
| 框架 | Vue 3 | 页面与组件开发 |
| 语言 | TypeScript | 类型约束 |
| 构建工具 | Vite | 本地开发和构建 |
| 路由 | Vue Router | 登录页、注册页、进度列表等页面路由 |
| 状态管理 | Pinia | 用户信息和认证状态 |
| HTTP | Axios | API 请求、Token 注入、自动刷新 |
| UI 组件 | Element Plus | 表格、分页、日期时间选择器、表单 |
| 单元测试 | Vitest | 工具函数、Store、组件测试 |
| 组件测试 | Vue Test Utils | Vue 组件交互测试 |
| API Mock | MSW | 隔离后端测试前端请求行为 |
| E2E | Playwright | 注册、登录、增删改查等核心流程 |
| 代码质量 | ESLint + Prettier | 代码检查与格式统一 |

### 4.2 后端

| 类型 | 技术 | 用途 |
|---|---|---|
| 语言 | Python 3.12+ | 后端开发 |
| Web 框架 | Django | ORM、用户模型、管理能力 |
| API 框架 | Django REST Framework | Serializer、ViewSet、权限、分页 |
| JWT | djangorestframework-simplejwt | Access/Refresh Token 与黑名单 |
| 数据库 | PostgreSQL | 持久化业务数据 |
| 跨域 | django-cors-headers | 本地开发跨域配置 |
| 过滤 | django-filter | 条件筛选和排序 |
| 测试 | pytest + pytest-django | 单元测试、数据库测试和 API 测试 |
| 覆盖率 | pytest-cov | 测试覆盖率 |
| API 文档 | drf-spectacular | OpenAPI 规范与 Swagger UI |
| 代码质量 | Ruff | 格式化与静态检查 |

版本应在项目初始化时锁定到依赖文件中；小版本升级需要先通过完整测试套件。

---

## 5. 仓库与目录设计

建议使用单仓库管理，前后端代码仍然保持物理分离：

```text
campus-recruitment-tracker/
├─ frontend/
│  ├─ src/
│  │  ├─ api/
│  │  │  ├─ auth.ts
│  │  │  ├─ applications.ts
│  │  │  └─ http.ts
│  │  ├─ components/
│  │  │  ├─ ApplicationForm.vue
│  │  │  ├─ ApplicationTable.vue
│  │  │  ├─ ConfirmDeleteDialog.vue
│  │  │  └─ StageTimeEditor.vue
│  │  ├─ layouts/
│  │  │  └─ MainLayout.vue
│  │  ├─ router/
│  │  │  └─ index.ts
│  │  ├─ stores/
│  │  │  └─ auth.ts
│  │  ├─ types/
│  │  │  ├─ api.ts
│  │  │  ├─ auth.ts
│  │  │  └─ application.ts
│  │  ├─ utils/
│  │  │  ├─ token-storage.ts
│  │  │  └─ datetime.ts
│  │  ├─ views/
│  │  │  ├─ LoginView.vue
│  │  │  ├─ RegisterView.vue
│  │  │  └─ ApplicationsView.vue
│  │  ├─ App.vue
│  │  └─ main.ts
│  ├─ tests/
│  │  ├─ unit/
│  │  ├─ component/
│  │  └─ e2e/
│  ├─ package.json
│  └─ vite.config.ts
├─ backend/
│  ├─ config/
│  │  ├─ settings/
│  │  │  ├─ base.py
│  │  │  ├─ development.py
│  │  │  ├─ test.py
│  │  │  └─ production.py
│  │  ├─ urls.py
│  │  └─ wsgi.py
│  ├─ apps/
│  │  ├─ accounts/
│  │  │  ├─ migrations/
│  │  │  ├─ serializers.py
│  │  │  ├─ urls.py
│  │  │  ├─ views.py
│  │  │  └─ tests/
│  │  └─ applications/
│  │     ├─ migrations/
│  │     ├─ filters.py
│  │     ├─ models.py
│  │     ├─ pagination.py
│  │     ├─ permissions.py
│  │     ├─ serializers.py
│  │     ├─ urls.py
│  │     ├─ views.py
│  │     └─ tests/
│  ├─ manage.py
│  ├─ pyproject.toml
│  └─ pytest.ini
├─ docs/
├─ .env.example
├─ docker-compose.yml
└─ README.md
```

---

## 6. 数据库设计

## 6.1 用户表

第一期直接使用 Django 自带用户模型 `django.contrib.auth.models.User`，减少认证风险和重复实现。注册时至少保存用户名和密码，可选保存邮箱。

密码不能明文保存。必须调用 Django 的 `set_password()` 或使用 `User.objects.create_user()`，由 Django 进行加盐哈希。

主要字段如下：

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | 主键 | 用户 ID |
| username | varchar(150) | 唯一、非空 | 登录用户名 |
| password | varchar(128) | 非空 | Django 密码哈希 |
| email | varchar(254) | 可空 | 邮箱 |
| is_active | boolean | 默认 true | 是否允许登录 |
| date_joined | timestamptz | 非空 | 注册时间 |

如果产品确定必须使用邮箱登录，应在开发前改为自定义用户模型，并在第一次迁移前完成；Django 项目投入使用后再切换用户模型成本很高。

## 6.2 公司投递记录表

表名建议：`job_applications`。

| 字段 | PostgreSQL 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| id | bigint | 主键、自增 | 记录 ID |
| user_id | bigint | 外键、非空、级联删除 | 所属用户 |
| company_name | varchar(200) | 非空 | 公司名称 |
| position_name | varchar(200) | 非空 | 投递岗位 |
| application_status | varchar(30) | 默认 `applied` | 当前状态 |
| application_time | timestamptz | 可空 | 投递时间 |
| ai_interview_time | timestamptz | 可空、默认 null | AI 面时间 |
| written_test_time | timestamptz | 可空、默认 null | 笔试时间 |
| first_interview_time | timestamptz | 可空、默认 null | 一面时间 |
| second_interview_time | timestamptz | 可空、默认 null | 二面时间 |
| third_interview_time | timestamptz | 可空、默认 null | 三面时间 |
| hr_interview_time | timestamptz | 可空、默认 null | HR 面时间 |
| notes | text | 空字符串 | 备注 |
| created_at | timestamptz | 自动写入 | 创建时间 |
| updated_at | timestamptz | 自动更新 | 最后修改时间 |

`application_status` 枚举建议：

| 值 | 展示名 | 含义 |
|---|---|---|
| applied | 已投递 | 已提交简历，尚未进入具体阶段 |
| in_progress | 流程中 | 已进入测评或面试流程 |
| offer | 已录用 | 已收到 Offer |
| rejected | 已拒绝 | 企业拒绝或流程终止 |
| withdrawn | 已放弃 | 用户主动终止 |

### 6.2.1 Django 模型草案

```python
class JobApplication(models.Model):
    class Status(models.TextChoices):
        APPLIED = "applied", "已投递"
        IN_PROGRESS = "in_progress", "流程中"
        OFFER = "offer", "已录用"
        REJECTED = "rejected", "已拒绝"
        WITHDRAWN = "withdrawn", "已放弃"

    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="job_applications",
    )
    company_name = models.CharField(max_length=200)
    position_name = models.CharField(max_length=200)
    application_status = models.CharField(
        max_length=30,
        choices=Status.choices,
        default=Status.APPLIED,
    )
    application_time = models.DateTimeField(null=True, blank=True)
    ai_interview_time = models.DateTimeField(null=True, blank=True)
    written_test_time = models.DateTimeField(null=True, blank=True)
    first_interview_time = models.DateTimeField(null=True, blank=True)
    second_interview_time = models.DateTimeField(null=True, blank=True)
    third_interview_time = models.DateTimeField(null=True, blank=True)
    hr_interview_time = models.DateTimeField(null=True, blank=True)
    notes = models.TextField(blank=True, default="")
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        db_table = "job_applications"
        ordering = ["-updated_at", "-id"]
        indexes = [
            models.Index(fields=["user", "-updated_at"]),
            models.Index(fields=["user", "application_status"]),
            models.Index(fields=["user", "company_name"]),
        ]
```

### 6.2.2 数据约束

- `company_name`、`position_name` 去除首尾空格后不能为空。
- 各阶段时间都允许为 `null`，对应“尚未填写”。
- 前端清空时间时提交 JSON `null`，不能提交空字符串。
- 同一用户可以向同一公司投递不同岗位，因此不对公司名做唯一约束。
- 同一用户也可能重复投递同一岗位，因此第一期不做 `(user, company_name, position_name)` 唯一约束。
- 删除用户时级联删除其投递记录。
- 不接受前端提交 `user` 字段，所属用户只能由后端从 Token 中获取。

## 6.3 索引与搜索说明

第一期数据规模预计较小，可使用 PostgreSQL `icontains` 完成公司名和岗位名模糊搜索。查询始终先使用 `user_id` 限定数据范围。

当单用户数据量达到数万条时，可增加 `pg_trgm` 扩展和 GIN trigram 索引，以优化模糊搜索；该优化不作为第一期前置依赖。

---

## 7. 用户与认证功能设计

## 7.1 注册

### 7.1.1 页面字段

- 用户名：必填，3～30 个字符。
- 邮箱：选填；填写时必须符合邮箱格式。
- 密码：必填，至少 8 个字符，并执行 Django 密码校验器。
- 确认密码：必填，必须与密码一致。

### 7.1.2 后端处理流程

1. Serializer 校验字段。
2. 检查两次密码是否一致。
3. 检查用户名是否已存在。
4. 调用 Django 密码校验器，拒绝过弱密码。
5. 使用 `create_user()` 写入 PostgreSQL。
6. 返回新用户的安全字段，不返回密码和密码哈希。

注册成功后不自动登录，前端跳转登录页并提示“注册成功，请登录”。这样注册和登录职责清晰，测试也更稳定。

### 7.1.3 异常处理

- 用户名重复：`409 USERNAME_ALREADY_EXISTS`。
- 两次密码不一致：`400 PASSWORDS_DO_NOT_MATCH`。
- 密码过弱：`400 WEAK_PASSWORD`。
- 字段格式错误：`400 VALIDATION_ERROR`，并在 `details` 中返回字段错误。

## 7.2 登录

### 7.2.1 Token 策略

采用 Access Token + Refresh Token：

- Access Token 有效期：30 分钟。
- Refresh Token 有效期：7 天。
- 开启 Refresh Token rotation，每次刷新返回新的 Refresh Token。
- 开启旧 Refresh Token 黑名单，旧 Token 刷新后立即失效。
- 用户主动退出时，将当前 Refresh Token 加入黑名单。

“浏览器缓存时间 7 天”以 Refresh Token 的 7 天绝对有效期为准。前端本地记录 `expiresAt`，超过该时间主动清除登录态。

### 7.2.2 浏览器存储方案

为严格满足“Token 保存在浏览器缓存”的需求，第一期将 Token 封装在统一的 `token-storage.ts` 中，使用 `localStorage` 保存：

```json
{
  "accessToken": "...",
  "refreshToken": "...",
  "expiresAt": 1790000000000
}
```

不得在组件中直接读写 `localStorage`。所有读写必须经过统一封装，以便后续升级为更安全的 HttpOnly Cookie 方案。

安全注意：`localStorage` 中的 Token 可能被 XSS 读取，因此必须禁止危险 HTML 注入，不使用未经净化的 `v-html`，配置安全响应头，并严格审查第三方前端依赖。生产环境更推荐把 Refresh Token 放进 `Secure + HttpOnly + SameSite` Cookie；如果采用该增强方案，前端只在内存中保存 Access Token。

### 7.2.3 登录流程

1. 用户输入用户名和密码。
2. 前端提交登录接口。
3. 后端验证用户和密码，返回 Access Token、Refresh Token 和用户信息。
4. 前端调用统一 Token 存储模块保存 Token 和 7 天过期时间。
5. Pinia 保存用户信息。
6. 跳转公司进度列表页。

## 7.3 自动刷新

Axios 请求拦截器为受保护请求注入：

```http
Authorization: Bearer <access_token>
```

响应拦截器收到 `401` 后执行：

1. 若请求本身是刷新接口，则不再刷新，直接退出登录。
2. 若没有 Refresh Token 或本地 7 天期限已过，则清空登录态并跳转登录页。
3. 若当前没有刷新任务，则调用刷新接口。
4. 若多个请求同时收到 401，只允许发起一次刷新；其他请求进入等待队列。
5. 刷新成功后更新 Access Token 和轮换后的 Refresh Token。
6. 使用新 Access Token 重试原请求一次。
7. 刷新失败则清空 Token、用户状态和等待队列，跳转登录页。

原请求必须使用 `_retry` 标志防止无限重试。

## 7.4 退出

退出流程：

1. 前端向退出接口提交 Refresh Token。
2. 后端将该 Refresh Token 加入黑名单。
3. 无论后端退出请求成功还是因网络失败，前端都清除本地 Token 和用户状态。
4. 跳转登录页。
5. 浏览器后退时，路由守卫仍应阻止访问受保护页面。

退出接口应设计为幂等：Token 已失效时也可以视作退出完成，避免用户被困在异常状态。

## 7.5 登录态恢复与路由保护

页面刷新时：

1. Pinia 从统一存储模块读取 Token。
2. 如果 7 天有效期已过，立即清空并进入登录页。
3. 如果 Access Token 存在，调用当前用户接口校验登录态。
4. 如果 Access Token 过期，Axios 自动刷新后重试当前用户接口。
5. 获取用户成功后再进入受保护页面，避免页面闪烁和未授权请求。

路由元信息：

```ts
meta: { requiresAuth: true }
```

路由守卫规则：

- 未登录访问受保护页面：跳转 `/login?redirect=<原地址>`。
- 已登录访问登录或注册页：跳转 `/applications`。
- 初始化认证状态期间显示全屏加载状态。

---

## 8. 秋招进度管理功能设计

## 8.1 列表页布局

列表页由以下区域组成：

1. 顶部导航：项目名称、当前用户名、退出按钮。
2. 查询区：关键字、投递状态、当前阶段、开始/结束时间、查询、重置。
3. 操作区：新增公司进度按钮。
4. 数据表格：公司、岗位、状态、各阶段时间、更新时间、操作。
5. 分页区：总条数、当前页、每页数量、上一页、下一页、页码跳转。

桌面端表格列较多，应固定公司/岗位列和操作列，中间阶段列支持横向滚动。窄屏设备可改为卡片展示，但不影响接口设计。

## 8.2 新增记录

点击“新增公司进度”打开抽屉或对话框。

字段：

- 公司名称：必填。
- 岗位名称：必填。
- 投递状态：默认“已投递”。
- 投递时间：可空。
- AI 面时间：可空。
- 笔试时间：可空。
- 一面时间：可空。
- 二面时间：可空。
- 三面时间：可空。
- HR 面时间：可空。
- 备注：可空。

时间字段统一使用日期时间选择器，同时允许键盘输入。提交前将时间转换成 ISO 8601 字符串；未填写的字段提交 `null`。

成功后：

- 关闭表单。
- 清空表单状态。
- 回到第 1 页并重新请求列表，确保新记录可见。
- 显示成功提示。

失败后保留用户已输入内容，并在对应字段展示后端校验错误。

## 8.3 查看与搜索

### 8.3.1 默认查询

- 仅返回当前用户的数据。
- 默认按 `updated_at DESC, id DESC` 排序。
- 默认第 1 页，每页 20 条。

### 8.3.2 搜索

`search` 同时模糊匹配：

- 公司名称。
- 岗位名称。

搜索框按下 Enter 或点击“查询”时请求；为避免每次按键都请求后端，也可以使用 300ms 防抖，但必须确保取消旧请求或只接收最新响应。

### 8.3.3 筛选

- `application_status`：按投递状态筛选。
- `stage`：筛选某阶段时间是否已填写。
- `application_time_after`：投递时间下限。
- `application_time_before`：投递时间上限。

当前阶段是展示派生字段，不单独持久化。建议按以下优先级从已有的最晚阶段推导：

```text
HR 面 > 三面 > 二面 > 一面 > 笔试 > AI 面 > 已投递
```

若 `application_status` 是 `offer`、`rejected` 或 `withdrawn`，页面优先展示该终态，而不是派生阶段。

### 8.3.4 排序

允许排序字段：

- `created_at`
- `updated_at`
- `application_time`
- `ai_interview_time`
- `written_test_time`
- `first_interview_time`
- `second_interview_time`
- `third_interview_time`
- `hr_interview_time`

后端必须使用白名单控制排序字段，不能直接将任意用户输入传给 ORM。

## 8.4 修改记录

支持两种修改入口：

1. 点击“编辑”，打开完整表单修改全部可编辑字段。
2. 点击某个阶段时间单元格，快速选择、输入或清空时间。

完整更新使用 `PATCH`，只提交发生变化的字段。后端根据 Serializer 做部分更新。

更新成功后：

- 使用接口返回数据更新当前行，或重新请求当前页。
- 显示成功提示。
- 保留当前页、查询条件、排序条件和每页数量。

并发更新第一期采用“最后一次写入生效”。若未来需要严格防止覆盖，可引入 `version` 字段或基于 `updated_at` 的乐观锁。

## 8.5 删除记录

删除前必须显示确认对话框，内容包含公司名称和岗位，防止误删。

删除成功后：

- 重新查询当前页。
- 如果删除的是当前页最后一条且当前页大于 1，则页码减 1 后重新查询。
- 显示删除成功提示。

第一期采用硬删除。若后续需要回收站，应增加 `deleted_at` 并改为软删除，而不是在现有逻辑中临时隐藏。

## 8.6 数据隔离

数据隔离必须在 QuerySet 层统一实现：

```python
def get_queryset(self):
    return JobApplication.objects.filter(user=self.request.user)
```

创建时由后端写入用户：

```python
def perform_create(self, serializer):
    serializer.save(user=self.request.user)
```

这样即使恶意用户猜到其他记录 ID：

- 获取详情返回 404。
- 修改返回 404。
- 删除返回 404。

不能返回 403 并暴露“该 ID 确实存在”。

---

## 9. REST API 设计

## 9.1 通用约定

- Base URL：`/api/v1`
- Content-Type：`application/json`
- 受保护接口：`Authorization: Bearer <access_token>`
- 日期时间：ISO 8601，例如 `2026-09-20T09:30:00+08:00`
- 空时间：JSON `null`
- 列表响应：统一分页结构
- 错误响应：统一错误码和可读消息

统一错误格式：

```json
{
  "code": "VALIDATION_ERROR",
  "message": "请求参数校验失败",
  "details": {
    "company_name": ["公司名称不能为空"]
  }
}
```

## 9.2 注册接口

```http
POST /api/v1/auth/register/
```

请求：

```json
{
  "username": "zhangsan",
  "email": "zhangsan@example.com",
  "password": "StrongPass_123",
  "password_confirm": "StrongPass_123"
}
```

成功：`201 Created`

```json
{
  "id": 1,
  "username": "zhangsan",
  "email": "zhangsan@example.com",
  "date_joined": "2026-09-20T08:00:00Z"
}
```

## 9.3 登录接口

```http
POST /api/v1/auth/login/
```

请求：

```json
{
  "username": "zhangsan",
  "password": "StrongPass_123"
}
```

成功：`200 OK`

```json
{
  "access": "<jwt-access-token>",
  "refresh": "<jwt-refresh-token>",
  "access_expires_in": 1800,
  "refresh_expires_in": 604800,
  "user": {
    "id": 1,
    "username": "zhangsan",
    "email": "zhangsan@example.com"
  }
}
```

用户名或密码错误统一返回 `401 INVALID_CREDENTIALS`，不分别说明用户名是否存在。

## 9.4 刷新接口

```http
POST /api/v1/auth/refresh/
```

请求：

```json
{
  "refresh": "<current-refresh-token>"
}
```

成功：`200 OK`

```json
{
  "access": "<new-access-token>",
  "refresh": "<rotated-refresh-token>",
  "access_expires_in": 1800,
  "refresh_expires_in": 604800
}
```

## 9.5 退出接口

```http
POST /api/v1/auth/logout/
Authorization: Bearer <access-token>
```

请求：

```json
{
  "refresh": "<current-refresh-token>"
}
```

成功：`204 No Content`

## 9.6 当前用户接口

```http
GET /api/v1/auth/me/
```

成功：`200 OK`

```json
{
  "id": 1,
  "username": "zhangsan",
  "email": "zhangsan@example.com"
}
```

## 9.7 投递记录列表

```http
GET /api/v1/applications/
```

查询参数：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| page | int | 1 | 页码，最小 1 |
| page_size | int | 20 | 每页数量，允许 10/20/50/100，最大 100 |
| search | string | 空 | 公司名或岗位名模糊搜索 |
| application_status | string | 空 | 状态筛选 |
| stage | string | 空 | `ai_interview` 等阶段是否已填写 |
| application_time_after | datetime | 空 | 投递时间下限 |
| application_time_before | datetime | 空 | 投递时间上限 |
| ordering | string | `-updated_at` | 排序字段，前缀 `-` 表示倒序 |

示例：

```http
GET /api/v1/applications/?page=2&page_size=50&search=腾讯&application_status=in_progress&ordering=-first_interview_time
```

成功：`200 OK`

```json
{
  "count": 126,
  "page": 2,
  "page_size": 50,
  "total_pages": 3,
  "next": "/api/v1/applications/?page=3&page_size=50",
  "previous": "/api/v1/applications/?page=1&page_size=50",
  "results": [
    {
      "id": 18,
      "company_name": "示例科技",
      "position_name": "后端开发工程师",
      "application_status": "in_progress",
      "current_stage": "first_interview",
      "application_time": "2026-09-01T10:00:00+08:00",
      "ai_interview_time": null,
      "written_test_time": "2026-09-05T19:00:00+08:00",
      "first_interview_time": "2026-09-10T14:30:00+08:00",
      "second_interview_time": null,
      "third_interview_time": null,
      "hr_interview_time": null,
      "notes": "技术岗提前批",
      "created_at": "2026-09-01T10:05:00+08:00",
      "updated_at": "2026-09-10T15:20:00+08:00"
    }
  ]
}
```

## 9.8 新增投递记录

```http
POST /api/v1/applications/
```

请求：

```json
{
  "company_name": "示例科技",
  "position_name": "后端开发工程师",
  "application_status": "applied",
  "application_time": "2026-09-20T10:00:00+08:00",
  "ai_interview_time": null,
  "written_test_time": null,
  "first_interview_time": null,
  "second_interview_time": null,
  "third_interview_time": null,
  "hr_interview_time": null,
  "notes": "官网投递"
}
```

成功：`201 Created`，返回完整记录。

## 9.9 获取详情

```http
GET /api/v1/applications/{id}/
```

成功：`200 OK`。记录不存在或不属于当前用户均返回 `404 NOT_FOUND`。

## 9.10 修改记录

```http
PATCH /api/v1/applications/{id}/
```

快速更新一面时间：

```json
{
  "first_interview_time": "2026-09-25T14:00:00+08:00"
}
```

清空一面时间：

```json
{
  "first_interview_time": null
}
```

成功：`200 OK`，返回更新后的完整记录。

## 9.11 删除记录

```http
DELETE /api/v1/applications/{id}/
```

成功：`204 No Content`。

---

## 10. 后端实现设计

## 10.1 ViewSet

投递记录使用 `ModelViewSet` 提供 CRUD，主要配置：

```python
class JobApplicationViewSet(ModelViewSet):
    serializer_class = JobApplicationSerializer
    permission_classes = [IsAuthenticated]
    pagination_class = JobApplicationPagination
    filter_backends = [DjangoFilterBackend, SearchFilter, OrderingFilter]
    filterset_class = JobApplicationFilter
    search_fields = ["company_name", "position_name"]
    ordering_fields = [
        "created_at",
        "updated_at",
        "application_time",
        "ai_interview_time",
        "written_test_time",
        "first_interview_time",
        "second_interview_time",
        "third_interview_time",
        "hr_interview_time",
    ]
    ordering = ["-updated_at", "-id"]

    def get_queryset(self):
        return JobApplication.objects.filter(user=self.request.user)

    def perform_create(self, serializer):
        serializer.save(user=self.request.user)
```

## 10.2 Serializer

- `user` 不暴露为可写字段。
- `id`、`current_stage`、`created_at`、`updated_at` 只读。
- 对公司名和岗位名执行 `strip()` 后非空校验。
- 时间字段接受 ISO 8601 或 `null`。
- `current_stage` 通过 SerializerMethodField 或模型属性计算。
- 不在 Serializer 内放复杂查询，防止列表 N+1。

## 10.3 分页

自定义 `PageNumberPagination`：

```python
class JobApplicationPagination(PageNumberPagination):
    page_size = 20
    page_size_query_param = "page_size"
    max_page_size = 100
```

如果产品只允许固定值 10/20/50/100，需要在分页类中额外校验；不合法值返回 400，而不是静默接受。

## 10.4 数据库事务

- 单表 CRUD 由 Django 单次 ORM 操作保证原子性。
- 注册可使用 `transaction.atomic()`，便于未来增加用户配置表。
- Token 黑名单由 SimpleJWT 的 blacklist app 管理。
- 不在数据库事务中执行外部网络调用。

## 10.5 配置管理

敏感配置只能来自环境变量：

```text
DJANGO_SECRET_KEY=
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=
DATABASE_URL=postgresql://...
CORS_ALLOWED_ORIGINS=
```

`.env` 不提交 Git，只提交 `.env.example`。测试使用独立测试数据库配置，禁止连接开发或生产数据库。

## 10.6 日志

应记录：

- 请求方法、路径、状态码、耗时、请求 ID。
- 登录成功/失败，但不记录密码和完整 Token。
- 未处理异常堆栈。
- 重要数据修改的用户 ID 和记录 ID。

日志中必须脱敏 Authorization 头、密码和 Token。

---

## 11. 前端实现设计

## 11.1 TypeScript 类型

```ts
export type ApplicationStatus =
  | 'applied'
  | 'in_progress'
  | 'offer'
  | 'rejected'
  | 'withdrawn'

export interface JobApplication {
  id: number
  companyName: string
  positionName: string
  applicationStatus: ApplicationStatus
  currentStage: string
  applicationTime: string | null
  aiInterviewTime: string | null
  writtenTestTime: string | null
  firstInterviewTime: string | null
  secondInterviewTime: string | null
  thirdInterviewTime: string | null
  hrInterviewTime: string | null
  notes: string
  createdAt: string
  updatedAt: string
}
```

可在 Axios 层统一完成 snake_case 与 camelCase 转换；若不做转换，则前端类型直接使用后端字段名，避免项目中混用两套规则。

## 11.2 Pinia 认证 Store

状态：

```ts
interface AuthState {
  user: User | null
  initialized: boolean
  loading: boolean
}
```

操作：

- `register(payload)`：注册。
- `login(credentials)`：登录并保存 Token。
- `fetchMe()`：恢复用户信息。
- `logout()`：调用退出接口并清理本地状态。
- `initialize()`：应用启动时恢复登录态。

Token 不重复放入 Pinia 持久化插件，避免出现两份状态源。Token 的唯一来源是 `token-storage.ts`。

## 11.3 HTTP 客户端

建议拆分：

- `publicClient`：注册、登录、刷新，不挂载自动刷新响应拦截器。
- `authClient`：业务接口，自动注入 Access Token 和处理 401。

这样可以防止刷新请求再次触发刷新，形成死循环。

## 11.4 列表查询状态

列表页维护：

```ts
interface ApplicationQuery {
  page: number
  pageSize: 10 | 20 | 50 | 100
  search: string
  applicationStatus?: ApplicationStatus
  stage?: string
  applicationTimeAfter?: string
  applicationTimeBefore?: string
  ordering: string
}
```

查询条件同步到 URL query。这样刷新页面、复制链接或浏览器前进后退时，可以恢复当前列表状态。

规则：

- 修改搜索或筛选条件时，`page` 重置为 1。
- 修改每页数量时，`page` 重置为 1。
- 修改排序时，保留筛选条件并将 `page` 重置为 1。
- 仅翻页时，不清空其他条件。

## 11.5 表单设计

新增和编辑共用 `ApplicationForm.vue`，通过 `mode: 'create' | 'edit'` 区分行为。

表单状态必须使用传入数据的副本，不能直接修改表格行对象；只有接口成功后才更新列表，避免请求失败时 UI 显示未保存的数据。

提交期间：

- 禁用重复提交。
- 按钮显示加载状态。
- 后端字段错误映射到对应表单项。
- 关闭表单前如果有未保存修改，应二次确认。

## 11.6 空状态与异常状态

- 初次无记录：展示引导文案和“新增第一条记录”。
- 查询无结果：提示调整筛选条件，提供“重置筛选”。
- 网络错误：保留查询状态，显示重试按钮。
- 401：自动刷新或回到登录页。
- 403：提示没有操作权限。
- 404：提示记录不存在或已被删除，并刷新列表。
- 500：显示通用错误，不暴露后端堆栈。

---

## 12. 安全设计

1. 密码只由 Django 密码哈希系统存储。
2. 所有业务接口强制 `IsAuthenticated`。
3. 所有记录 QuerySet 强制按 `request.user` 过滤。
4. Refresh Token 开启轮换和黑名单。
5. 生产环境只允许 HTTPS。
6. CORS 只允许明确的前端域名，不允许任意来源。
7. 不在日志、错误信息或监控中输出密码和 Token。
8. 登录和注册接口增加基于 IP/用户名的限流。
9. 后端对所有字段再次校验，不能信任前端。
10. Django `SECRET_KEY`、数据库密码放在环境变量或密钥系统中。
11. 前端避免 `v-html`；必须使用时先进行可靠净化。
12. 配置 CSP、`X-Content-Type-Options`、`Referrer-Policy` 等安全头。
13. 错误登录统一提示，防止枚举用户名。
14. API 返回最少必要用户数据。
15. 定期更新依赖并执行依赖漏洞扫描。

---

## 13. TDD 开发方案

## 13.1 TDD 循环

每个功能严格执行：

1. **Red**：先写一个描述业务行为的测试，并确认它因功能尚未实现而失败。
2. **Green**：只写让测试通过所需的最小实现。
3. **Refactor**：在测试保护下消除重复、改善命名和结构。
4. 运行受影响测试与完整回归测试。
5. 小步提交，每个提交保持可运行。

测试不能在实现完成后补写，否则不属于真正的 TDD。

## 13.2 后端测试分层

### 模型测试

- 默认状态为 `applied`。
- 六个阶段时间默认均为 `None`。
- `created_at`、`updated_at` 正确生成。
- 删除用户时级联删除投递记录。
- 当前阶段派生逻辑覆盖每个阶段和空记录。

### Serializer 测试

- 公司名和岗位名必填。
- 只包含空格的名称被拒绝。
- 合法 ISO 8601 时间可保存。
- 时间允许 `null`。
- 非法时间格式返回字段错误。
- 非法状态值被拒绝。
- `user`、`id`、时间戳不可由客户端覆盖。

### 认证 API 测试

- 注册成功后数据库中存在用户。
- 密码保存为哈希，且 `check_password()` 成功。
- 重复用户名注册失败。
- 弱密码注册失败。
- 两次密码不一致失败。
- 正确密码登录成功并返回两个 Token。
- 错误密码登录失败且不泄露用户名存在性。
- Access Token 可以访问受保护接口。
- Refresh Token 可获得新 Access Token。
- Token 轮换后旧 Refresh Token 进入黑名单。
- 退出后 Refresh Token 无法再次刷新。
- 未认证访问 `/me/` 返回 401。

### 投递 API 测试

- 未登录访问 CRUD 全部返回 401。
- 用户可以新增自己的记录。
- 请求体伪造 `user` 不能改变记录归属。
- 列表只返回当前用户记录。
- 用户不能查看、修改、删除其他用户记录。
- 新增时六个阶段时间可全部为空。
- 每个阶段时间可以分别设置和清空。
- PATCH 只修改提交字段。
- 删除后数据库记录不存在。
- 公司名和岗位名搜索有效。
- 状态和阶段筛选有效。
- 排序有效。
- 默认分页为 20。
- `page_size` 可设为 10、20、50、100。
- 超过最大每页数量被拒绝或限制为 100，行为必须固定并测试。
- 页码超出范围返回统一行为。
- 列表查询数量固定，防止意外 N+1。

### 后端测试样例

```python
@pytest.mark.django_db
def test_user_cannot_update_another_users_application(
    api_client, user_factory, application_factory
):
    owner = user_factory()
    attacker = user_factory()
    application = application_factory(user=owner)
    api_client.force_authenticate(attacker)

    response = api_client.patch(
        f"/api/v1/applications/{application.id}/",
        {"company_name": "恶意修改"},
        format="json",
    )

    assert response.status_code == 404
    application.refresh_from_db()
    assert application.company_name != "恶意修改"
```

## 13.3 前端测试分层

### 工具函数测试

- Token 保存、读取和清除。
- 7 天本地有效期判断。
- 空时间和 ISO 时间转换。
- 查询参数序列化。

### Store 测试

- 登录成功保存 Token 和用户信息。
- 登录失败不污染已有状态。
- 初始化时恢复用户。
- 本地期限过期时清空登录态。
- 退出时无论接口成功与否均清理本地状态。

### HTTP 拦截器测试

- 请求正确添加 Bearer Token。
- 401 后刷新并重试一次。
- 多个并发 401 只触发一次刷新请求。
- 刷新成功后等待请求全部继续。
- 刷新失败后清空 Token 并跳转登录。
- 刷新接口 401 不进入无限循环。

### 组件测试

- 注册表单校验密码一致性。
- 登录按钮在提交期间禁用。
- 新增表单阶段时间默认为空。
- 编辑表单正确回填。
- 清空日期提交 `null`。
- 删除前出现确认对话框。
- 分页控件改变页码后发出正确查询。
- 改变每页数量后回到第 1 页。
- 搜索/筛选状态正确保留。
- API 字段错误显示在对应表单项。

### E2E 测试

至少覆盖：

1. 注册 → 登录 → 进入列表。
2. 新增记录 → 列表可见 → 数据刷新后仍存在。
3. 更新笔试和一面时间 → 页面刷新后仍正确。
4. 清空某阶段时间。
5. 添加多条记录 → 搜索 → 筛选 → 翻页 → 调整每页数量。
6. 删除记录并确认列表更新。
7. Access Token 过期 → 自动刷新 → 原请求成功。
8. 退出 → 无法再访问受保护页面。

## 13.4 测试命名规范

测试名称应直接描述行为：

```text
test_register_persists_user_with_hashed_password
test_application_list_only_returns_current_users_records
test_refresh_failure_clears_cached_tokens
it_resets_page_to_one_when_page_size_changes
```

避免使用 `test_case_1` 等无法表达需求的名称。

## 13.5 覆盖率门槛

- 后端总体覆盖率：不低于 90%。
- 认证、权限、Token 刷新、数据隔离：分支覆盖率 100%。
- 前端总体覆盖率：不低于 80%。
- Token 存储、刷新拦截器、认证 Store：分支覆盖率 100%。
- 覆盖率只是底线，关键业务断言优先于单纯追求数字。

---

## 14. 推荐 TDD 实施顺序

## 第 0 阶段：工程基线

1. 初始化前端、后端和 PostgreSQL 开发环境。
2. 配置 Ruff、ESLint、Prettier、pytest、Vitest。
3. 配置测试数据库和 CI。
4. 写健康检查接口测试，再实现 `/api/v1/health/`。

完成标准：本地和 CI 均可运行空测试套件与健康检查。

## 第 1 阶段：注册

1. 写注册成功持久化测试。
2. 写密码哈希测试。
3. 写重复用户名、弱密码、密码不一致测试。
4. 最小实现 Serializer 和注册接口。
5. 写前端注册表单测试，再实现页面。

完成标准：用户可以注册，重启服务后数据仍在 PostgreSQL 中。

## 第 2 阶段：登录、刷新和退出

1. 写登录成功/失败 API 测试。
2. 实现 JWT 登录。
3. 写 7 天 Refresh Token 和轮换测试。
4. 实现刷新接口。
5. 写黑名单退出测试并实现退出。
6. 写前端 Token 存储与拦截器测试，再实现自动刷新。
7. 写路由守卫测试并实现登录态恢复。

完成标准：Access Token 过期后用户无感刷新；7 天过期或退出后必须重新登录。

## 第 3 阶段：投递模型与新增

1. 写模型默认值和迁移测试。
2. 实现模型与数据库迁移。
3. 写新增接口权限和校验测试。
4. 实现 Serializer 和 Create API。
5. 写前端新增表单测试并实现。

完成标准：新增记录持久化，六个阶段默认都为空。

## 第 4 阶段：列表、搜索和分页

1. 写数据隔离测试。
2. 写默认分页和可调每页数量测试。
3. 写搜索、筛选、排序测试。
4. 实现 QuerySet、FilterSet 和 Pagination。
5. 写前端列表、查询区和分页组件测试，再实现页面。

完成标准：列表只显示本人数据，分页和每页数量调整正确。

## 第 5 阶段：修改与删除

1. 写 PATCH、清空时间、跨用户修改测试。
2. 实现更新。
3. 写删除和跨用户删除测试。
4. 实现删除。
5. 写前端编辑、快速更新时间、删除确认测试，再实现交互。

完成标准：所有字段可更新，阶段时间可填写或清空，删除受权限保护。

## 第 6 阶段：E2E 与发布准备

1. 完成核心 E2E 流程。
2. 执行安全配置检查。
3. 执行数据库迁移演练和备份恢复演练。
4. 完成 API 文档和部署说明。
5. 执行完整回归测试。

---

## 15. CI 质量门禁

每次提交或合并请求执行：

```text
后端 Ruff 检查
        ↓
后端 pytest + coverage
        ↓
前端 ESLint + TypeScript 类型检查
        ↓
前端 Vitest + coverage
        ↓
前端生产构建
        ↓
核心 Playwright E2E
```

任一步失败都禁止合并。数据库测试使用临时 PostgreSQL 服务，不使用 SQLite 替代，因为 PostgreSQL 的时区、查询和索引行为需要在真实数据库上验证。

---

## 16. 部署设计

推荐容器化部署：

- 前端构建为静态资源，由 Nginx 托管。
- Nginx 将 `/api/` 反向代理到 Django。
- Django 使用 Gunicorn 或同类 WSGI 服务运行。
- PostgreSQL 使用独立持久化存储。
- 数据库迁移作为发布步骤执行，不在每个 Web 容器启动时并发执行。

生产发布顺序：

1. 备份数据库。
2. 构建前后端制品。
3. 在预发布环境执行迁移和完整测试。
4. 生产执行向后兼容的数据库迁移。
5. 发布后端。
6. 发布前端。
7. 健康检查与核心接口冒烟测试。
8. 监控错误率和响应时间。

备份要求：PostgreSQL 每日自动备份，并定期验证恢复流程；未验证可恢复的备份不能视为有效备份。

---

## 17. 验收标准

### 用户功能

- 注册成功后用户记录实际存在于 PostgreSQL。
- 数据库中不存在明文密码。
- 正确账号密码可登录，错误凭据无法登录。
- Token 登录态最长保持 7 天。
- Access Token 过期时可自动刷新，用户正常操作不中断。
- Refresh Token 过期或失效后回到登录页。
- 退出后本地 Token 被清理，Refresh Token 无法再次使用。
- 页面刷新后登录态可正确恢复。

### 秋招进度功能

- 用户可新增、查看、搜索、筛选、修改、删除自己的投递记录。
- AI 面、笔试、一面、二面、三面、HR 面默认均为空。
- 每个阶段时间均可通过选择器或文本输入设置。
- 已填写的阶段时间可以清空。
- 默认分页正常，用户可切换每页 10、20、50、100 条。
- 修改每页数量和筛选条件时页码逻辑正确。
- 用户无法通过修改 URL 或请求体访问其他用户数据。
- 刷新浏览器或重启服务后，已保存数据仍然存在。

### 质量标准

- 所有测试通过。
- 关键认证与权限代码达到约定覆盖率。
- OpenAPI 文档与实际接口一致。
- 前后端无阻断级静态检查错误。
- 生产环境不暴露调试信息、密码、密钥或 Token。

---

## 18. 关键决策与后续扩展

### 18.1 已确定决策

- 使用 REST API，不在第一期引入 GraphQL。
- 使用 JWT Access/Refresh 双 Token。
- Refresh Token 有效期 7 天，Access Token 30 分钟。
- 使用 Token 轮换和黑名单实现安全退出。
- 六个流程阶段直接作为可空时间字段存储，符合当前固定阶段需求。
- 当前阶段由已有时间字段派生，不重复存储。
- 第一期开启硬删除，不实现回收站。
- 每页最大 100 条，避免超大查询拖慢系统。

### 18.2 未来扩展方向

- 将固定阶段重构为独立 `application_stages` 表，支持自定义流程。
- 增加投递渠道、薪资、工作地点、官网链接、内推人等字段。
- 增加面试提醒与日历同步。
- 增加数据看板和 Offer 转化统计。
- 增加软删除和操作审计日志。
- 将 Refresh Token 从 localStorage 迁移到 HttpOnly Cookie。
- 增加邮箱验证、找回密码和多因素认证。

---

## 19. 开发完成定义（Definition of Done）

一个功能只有同时满足以下条件才算完成：

1. 验收行为已转化为自动化测试。
2. 测试在实现前确认失败，并在实现后通过。
3. 正常路径、错误路径、权限边界均有测试。
4. 前后端接口契约一致。
5. 数据库迁移已生成、审查并在 PostgreSQL 上验证。
6. 静态检查、类型检查和完整测试通过。
7. API 或用户行为变化已更新文档。
8. 不记录或暴露任何密码、密钥和 Token。
9. 代码经过重构，无明显重复和无效分支。
10. 核心用户流程通过 E2E 测试。

本方案可直接作为项目初始化、任务拆分、接口联调和测试验收的开发基线。
