# WEB-APP-002 验收记录

- 状态：`DONE`
- 验收结论：`ACCEPTED`
- 验收角色：项目管理 Agent
- 验收日期：2026-09-21
- 任务单：`docs/tasks/WEB-APP-002.md`
- 初始 RED 报告：`docs/test-reports/WEB-APP-002-red.md`
- 独立复测失败报告：`docs/test-reports/WEB-APP-002-failed.md`
- 最终测试通过报告：`docs/test-reports/WEB-APP-002-passed.md`
- 实现说明：`docs/implementation-notes/WEB-APP-002.md`
- 验收环境：Node.js `20.19.0`；npm `10.8.2`；Vitest `4.1.11`

## 系统设计契约对照

- 列表页提供项目名、当前用户名、退出和新增入口；查询区包含关键字、状态、阶段、投递时间范围、排序、查询和重置。
- 列表请求使用当前 query 和分页状态，默认第 1 页/20 条；筛选、排序、page size 变化将页码重置为 1，翻页保留其余条件。
- 查询参数遵循投递 API 契约，URL query 可恢复并支持浏览器前进/后退；搜索、筛选和排序进入列表请求。
- 初次空列表、筛选无结果、网络错误和 401 均展示可理解状态并保留查询；并发搜索只接受最新响应。

## 标准与证据

- [x] 标准 1：列表请求和展示严格使用当前查询参数及分页响应。最终独立测试覆盖 query/分页请求、公司/岗位/状态、六阶段时间和更新时间展示。
- [x] 标准 2：搜索、状态/阶段/时间筛选、排序和 page size 交互可观察，查询时提交完整参数且重置页码正确。最终测试确认这些参数进入请求，并验证页码重置与翻页保留条件。
- [x] 标准 3：URL 初始加载、刷新及浏览器前进/后退可恢复查询状态。最终独立测试覆盖 URL query 初始恢复和浏览器后退恢复。
- [x] 标准 4：加载、初次空列表、无结果和网络错误不会丢失用户查询，401 有明确状态；并发搜索旧响应不会覆盖新结果。最终独立测试覆盖全部状态和最新响应保护。
- [x] 标准 5：组件测试先 RED，失败回流后由测试 Agent 独立复测。初始 RED 记录 `ApplicationsView.vue` 缺失导致无法执行；`TEST_FAILED` 记录了真实的筛选/排序参数未进入请求，并区分了 jsdom `:has-text()` 选择器问题及测试路由夹具修正；实现说明为 `READY_FOR_TEST`；最终报告为 `TEST_PASSED`。

## 回流与质量门禁

- [x] 初始独立复测的 3 项 `:has-text()` 失败确认为测试工具选择器问题，改为标准 CSS/Vue Test Utils 公开定位并补齐登录路由夹具，未放宽断言。
- [x] 选择器修正后仍有 1 项真实实现失败：状态、阶段、时间范围和 ordering 未进入最终列表请求；该问题已回流实现并由最终独立复测验证修复。
- [x] 最终 5 个测试文件全部通过，共 `42 passed`，覆盖 WEB-APP-002 及 WEB-AUTH-001/002/003、WEB-APP-001 回归；npm ci 成功，命令退出码为 0。
- [x] 最终报告确认未修改 `frontend/src`；本次项目管理验收仅修改 `docs/acceptance/` 和 `docs/tasks/`。

## 结论

WEB-APP-002 的投递列表展示、查询参数提交、搜索/筛选/排序、分页与页码规则、URL 状态恢复、异常状态保留和并发响应保护均满足任务单及系统设计契约。回流问题已完成修正并通过独立复测，项目管理验收结论为 `ACCEPTED`，任务状态设置为 `DONE`。
