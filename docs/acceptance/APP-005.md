# APP-005 验收记录

- 状态：`DONE`
- 验收结论：`ACCEPTED`
- 验收角色：项目管理 Agent
- 验收日期：2026-09-21
- 任务单：`docs/tasks/APP-005.md`
- 初始 RED 报告：`docs/test-reports/APP-005-red.md`
- 回流报告：`docs/test-reports/APP-005-failed.md`
- 实现说明：`docs/implementation-notes/APP-005.md`
- 最终测试通过报告：`docs/test-reports/APP-005-passed.md`
- 验收环境：Windows；PostgreSQL 17.11（127.0.0.1:5432）；uv 0.7.21；Python 3.11.13；数据库凭据未写入本记录

## 系统设计搜索/筛选/排序契约对照

- `search` 同时模糊匹配公司名和岗位名，并在当前用户 QuerySet 内执行。
- `application_status`、`stage`、投递时间 after/before 提供筛选；`ordering` 仅允许创建/更新时间、投递时间及六个阶段时间字段，前缀 `-` 表示倒序。
- 查询可与分页组合；默认排序保持 `-updated_at,-id`，空时间排序位置固定；非法筛选和排序输入返回结构化 `400 VALIDATION_ERROR`。

## TDD 回流核对

- 初始 RED 报告记录专项 `20 failed, 1 passed`，失败来自搜索、筛选、时间范围、排序和非法参数公开行为缺失，且非环境问题。
- `APP-005-failed.md` 记录中途 `TEST_FAILED` 的 4 个失败，并逐项证明是测试断言与公开契约/夹具不一致：岗位名也应命中、状态筛选应返回全部匹配记录、时间 before 应按包含上限处理。
- 回流修正依据仅调整测试期望，不改变覆盖范围；最终通过报告明确生产实现未修改。

## 标准与证据

- [x] 标准 1：公司名和岗位名搜索均命中，且不会命中其他用户数据。最终专项 `21 passed`，确认 `Needle` 同时匹配当前用户的公司和岗位并排除另一用户。
- [x] 标准 2：状态、每个阶段已填写/未填写和投递时间范围筛选正确。最终专项覆盖五种状态、六个阶段、after/before 时间范围，结果 `21 passed`。
- [x] 标准 3：全部白名单排序字段正/倒序有效，默认排序稳定。最终专项覆盖创建/更新时间、投递时间和六个阶段字段正倒序，以及默认排序和空时间位置约定。
- [x] 标准 4：非法状态、阶段、时间或 ordering 被拒绝且错误结构稳定。最终专项确认非法参数返回 `400 VALIDATION_ERROR`，并覆盖字段错误结构。
- [x] 标准 5：组合筛选与分页可同时使用；测试先 RED 后实现。最终专项覆盖组合筛选、排序和分页；初始 RED、TEST_FAILED 修正依据、READY_FOR_TEST 实现说明和最终 `TEST_PASSED` 均齐全。

## 回归与质量门禁

- [x] `uv sync --frozen --group test` 成功。
- [x] APP-001 至 APP-005 完整 APP 回归 `61 passed`。
- [x] 实现说明记录 `manage.py check`、迁移漂移检查和迁移计划通过；本任务无模型或数据库变更。
- [x] 最终报告确认生产实现未修改；任务单、初始 RED、TEST_FAILED 回流依据、实现说明、最终通过报告和本验收记录齐全。
- [x] 本次仅修改 `docs/acceptance/` 和 `docs/tasks/`。

## 结论

APP-005 的搜索、筛选、时间范围、白名单排序、空时间排序、组合分页、非法参数校验和 TDD 回流复测均满足任务单及系统设计契约。项目管理验收结论为 `ACCEPTED`，任务状态设置为 `DONE`。
