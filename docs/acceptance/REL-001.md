# REL-001 验收记录

- 状态：`TEST_FAILED`
- 验收结论：`TEST_FAILED`
- 验收角色：项目管理 Agent
- 验收日期：2026-09-21
- 任务单：`docs/tasks/REL-001.md`
- RED 报告：`docs/test-reports/REL-001-red.md`
- 失败报告：`docs/test-reports/REL-001-failed.md`
- 关联 E2E 证据：`docs/test-reports/E2E-001-passed.md`；E2E-001 已由项目管理验收为 `DONE`
- 当前状态：未设为 `DONE`

## 已通过的发布门禁证据

- [x] 后端 Ruff 通过。
- [x] 后端 pytest smoke 通过。
- [x] 后端总体 coverage `91%`，达到 `>=90%` 门槛，coverage 命令退出码为 0。
- [x] 前端 `npm run lint` 和 `npm run build` 通过。
- [x] PostgreSQL 发布演练通过：`pg_dump`、迁移计划、隔离库 `rel001_restore` 恢复和 `job_applications` 数据探针均成功，恢复后行数 `558`。
- [x] 发布后 health、注册、登录和投递 smoke 通过。
- [x] 依赖任务 E2E-001 已独立 `7 passed` 并完成项目管理验收，本报告仅引用该既有证据，不冒充本轮重复执行。

## 门禁结果

- [x] 质量命令使用项目锁定的 uv/nvm/npm 环境，并在覆盖率失败时阻断；未将失败命令包装为通过。
- [ ] 全量前端 coverage 门槛通过：`npm run test:coverage` 的 59 tests 均通过，但 branches 仅 `73.51%`，低于配置的 `80%`；auth store branches 为 `60%`。
- [x] PostgreSQL 空库/迁移计划/备份恢复演练有可追溯输出，隔离恢复库数据探针通过。
- [x] 发布后 `/api/v1/health/`、认证和投递关键路径 smoke 通过。
- [x] 未通过的 coverage 门禁及其影响已明确记录；未宣称 REL-001 完成。

## 唯一剩余阻塞

前端分支覆盖率未达到发布门槛：总体 branches `73.51% < 80%`，auth store branches `60%`。这不是测试用例失败或环境超时，而是质量门禁的真实失败；因此 REL-001 不能设置为 `DONE`。

后续动作：测试 Agent 补齐 auth store/refresh 等未覆盖分支测试，或由项目管理 Agent 明确记录经批准的覆盖率豁免；随后重新执行 `npm run test:coverage` 并提交新的独立通过证据。除该项外，本轮记录的发布演练与关联回归均通过。

## 结论

REL-001 当前为 `TEST_FAILED`，唯一剩余阻塞是前端分支覆盖率门禁未达标。项目管理验收不通过，任务保持未完成；本轮仅修改 `docs/acceptance/` 和 `docs/tasks/`。
