# APP-003 实现说明

状态：READY_FOR_TEST

## 实现内容

- 新增投递列表接口 GET /api/v1/applications/，要求 Bearer Access Token。
- 新增本人投递详情接口 GET /api/v1/applications/{id}/，要求 Bearer Access Token。
- 列表 QuerySet 严格按 request.user 过滤，并按 updated_at DESC, id DESC 排序；user 查询参数不会改变隔离范围。
- 列表响应提供 count、page、page_size、total_pages、next、previous、results 分页容器。
- 详情返回完整投递字段（含派生的 current_stage）；他人记录与不存在记录统一返回 HTTP 404 和 {"code":"NOT_FOUND"}。
- 列表与创建共用认证边界，保留 APP-002 创建行为；未认证请求返回 401。
- 未新增模型字段，因此未产生迁移文件。

## 验证环境

- 工作区：G:\job
- Python：3.11.13
- uv：0.7.21
- 依赖同步：uv sync --frozen --group test 通过
- DATABASE_URL：postgresql://postgres:<password>@127.0.0.1:5432/postgres
- PostgreSQL：17.11，x86_64-windows，本次验证连接成功
- 测试数据库由项目配置隔离为 postgres_test

## 验证结果

- uv run --no-sync python manage.py check：通过
- uv run --no-sync python manage.py makemigrations --check --dry-run：通过，无变更
- uv run --no-sync python manage.py migrate --plan：通过，无待执行迁移
- APP-003 自测：7 passed
- 既有后端回归（APP-001/002、AUTH-001/002/003/004、DB-001、健康、URL）：58 passed

以上结果为实现 Agent 自测，交由测试 Agent 进行独立复测。
