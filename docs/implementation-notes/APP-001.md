# APP-001 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/apps/applications/、backend/config/settings.py、backend/apps/applications/migrations/0001_initial.py、docs/implementation-notes/APP-001.md；未修改 backend/tests、docs/test-reports、docs/tasks、验收文档或前端。
- 已实现行为：
  - 新增并注册 applications app 和 JobApplication 模型。
  - 数据表为 job_applications，user 外键绑定 Django User，on_delete=models.CASCADE，related_name 为 job_applications。
  - company_name、position_name 为最大 200 字符的必填字段；模型 clean() 去首尾空格并拒绝处理后为空的名称。
  - application_status 使用 applied、in_progress、offer、rejected、withdrawn 枚举，默认 applied。
  - application_time、ai_interview_time、written_test_time、first_interview_time、second_interview_time、third_interview_time、hr_interview_time 均为可空 DateTimeField；项目 USE_TZ 保证时区语义。
  - notes 默认空字符串；created_at 自动创建；updated_at 自动更新。
  - current_stage 为不落库 property，按 HR 面、三面、二面、一面、笔试、AI 面、已投递派生；offer、rejected、withdrawn 终态优先。
  - 声明 user+updated_at、user+application_status、user+company_name 三组索引，并保留默认 ordering。
- 数据库迁移：
  - 生成 backend/apps/applications/migrations/0001_initial.py。
  - PostgreSQL 开发数据库实际执行 uv run --no-sync python manage.py migrate --noinput 成功。
  - 随后 migrate --plan 显示 No planned migration operations。
- 配置变化：
  - applications app 加入 INSTALLED_APPS。
  - 保持 PostgreSQL、认证、注册、健康 API 和 JWT blacklist 配置不变。
- 已知限制：
  - 本任务只实现模型、迁移和 current_stage 派生，不实现 CRUD、分页、筛选或前端。
  - 必须使用可连接的 PostgreSQL DATABASE_URL，不允许 SQLite 兜底。
  - 本次会话使用的数据库地址为 postgresql://postgres:<password>@127.0.0.1:5432/postgres；密码未写入仓库，测试数据库保持独立。
- 自检结果：
  - uv：0.7.21；Python：3.11.13。
  - PostgreSQL：PostgreSQL 17.11，127.0.0.1:5432 accepting connections。
  - uv sync --frozen --group test：通过。
  - uv run --no-sync python manage.py check：通过，0 issues。
  - uv run --no-sync python manage.py makemigrations --check --dry-run：通过，No changes detected。
  - uv run --no-sync python manage.py migrate --noinput：通过。
  - uv run --no-sync python manage.py migrate --plan：通过，无待执行迁移。
  - uv run --no-sync pytest tests/test_app_001.py --tb=no -q：通过，10 passed。
  - AUTH-001/002/003/004、健康和 URL 回归：通过，33 passed。
- 建议复测命令：
  - $env:DATABASE_URL='postgresql://postgres:<password>@127.0.0.1:5432/postgres'
  - Set-Location backend
  - uv sync --frozen --group test
  - uv run --no-sync pytest tests/test_app_001.py --tb=no -q
- 测试完整性声明：未修改测试、断言或质量门槛。
