# APP-002 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/apps/applications/、backend/config/urls.py、docs/implementation-notes/APP-002.md；未修改 backend/tests、docs/test-reports、docs/tasks、验收文档或前端。
- 已实现行为：
  - 新增受保护的 POST /api/v1/applications/ 创建接口，使用 Bearer Access Token。
  - 使用 JobApplicationSerializer 返回完整 snake_case 记录：id、user、company_name、position_name、application_status、current_stage、application_time、六个阶段时间、notes、created_at、updated_at。
  - 通过 perform_create() 强制使用当前 Token 用户绑定记录；请求体 user 字段为只读并被忽略。
  - id、user、current_stage、created_at、updated_at 为只读。
  - 状态默认 applied，备注默认为空字符串，时间字段可省略或为 null。
  - 公司名和岗位名去首尾空格并拒绝空白；非法状态和非法 ISO 时间返回 400、code=VALIDATION_ERROR、details 按字段返回。
  - 未认证请求由 IsAuthenticated 拒绝为 401，不创建数据；响应不包含密码或 token。
- 数据库迁移：复用 APP-001 的 job_applications 迁移，无新增模型或迁移；makemigrations --check --dry-run 无变化。
- 配置变化：
  - applications URL 接入 config/urls.py。
  - 保持 PostgreSQL、认证、注册、登录、refresh、logout、健康 API 不变。
- 已知限制：
  - 本任务只实现创建，不实现列表、详情、更新、删除、分页或筛选。
  - 必须设置可连接的 PostgreSQL DATABASE_URL，不允许 SQLite 兜底。
  - 本次会话数据库地址为 postgresql://postgres:<password>@127.0.0.1:5432/postgres；密码未写入仓库，测试数据库保持独立。
- 自检结果：
  - uv：0.7.21；Python：3.11.13。
  - PostgreSQL：PostgreSQL 17.11，127.0.0.1:5432 accepting connections。
  - uv sync --frozen --group test：通过。
  - uv run --no-sync python manage.py check：通过，0 issues。
  - uv run --no-sync python manage.py makemigrations --check --dry-run：通过，No changes detected。
  - uv run --no-sync python manage.py migrate --plan：通过，无待执行迁移。
  - uv run --no-sync pytest tests/test_app_002.py --tb=no -q：通过，10 passed。
  - 认证、APP-001、健康和 URL 回归前轮结果：43 passed。
- 建议复测命令：
  - $env:DATABASE_URL='postgresql://postgres:<password>@127.0.0.1:5432/postgres'
  - Set-Location backend
  - uv sync --frozen --group test
  - uv run --no-sync pytest tests/test_app_002.py --tb=no -q
- 测试完整性声明：未修改测试、断言或质量门槛。
