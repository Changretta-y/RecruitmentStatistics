# DB-001 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/pyproject.toml、backend/uv.lock、backend/config/settings.py、backend/config/urls.py、backend/apps/health/；未修改 backend/tests、docs/test-reports、docs/tasks。
- 已实现行为：
  - 通过 uv 锁定 Django、Django REST framework、djangorestframework-simplejwt 和 psycopg[binary]。
  - 注册 DRF、JWT 黑名单应用和 JWTAuthentication 基线。
  - DATABASE_URL 使用 PostgreSQL URL 解析；未配置时保持 PostgreSQL 引擎配置，不回退 SQLite。
  - 开发数据库使用 URL 中的数据库名，测试数据库固定为 <开发数据库名>_test，本次实际为 postgres_test。
  - 保留 /api/v1/health/ JSON 健康接口。
- 数据库迁移：未新增业务模型，因此没有新增迁移文件；Django 内置 auth、contenttypes、sessions 和 token_blacklist 迁移可由公开命令规划和执行。
- 配置变化：
  - 运行时依赖和 uv.lock 已同步。
  - 使用用户提供的本机 PostgreSQL 会话凭据复测时，Django vendor 为 postgresql，engine 为 django.db.backends.postgresql。
- 已知限制：
  - 必须提供可连接的 PostgreSQL DATABASE_URL；数据库不可连接时命令会失败，不能使用 SQLite 兜底。
  - 复测连接串中的密码未写入仓库；文档中的示例使用脱敏占位符。
  - 本任务未实现注册、登录或投递业务。
- 自检结果：
  - uv：0.7.21；Python：3.11.13。
  - PostgreSQL：PostgreSQL 17.11 on x86_64-windows, compiled by msvc-19.44.35228, 64-bit。
  - uv sync --frozen：通过。
  - uv sync --frozen --group test：通过。
  - uv run --no-sync python manage.py check：通过，0 issues。
  - uv run --no-sync python manage.py makemigrations --check --dry-run：通过，No changes detected。
  - uv run --no-sync python manage.py migrate --plan：通过，内置迁移计划可生成。
  - uv run --group test --no-sync pytest tests/test_db_001.py --tb=no -q：通过，5 passed。
- 建议复测命令：
  - $env:DATABASE_URL='postgresql://postgres:<password>@127.0.0.1:5432/postgres'
  - Set-Location backend
  - uv sync --frozen --group test
  - uv run --group test --no-sync pytest tests/test_db_001.py --tb=no -q
- 测试完整性声明：未修改测试、断言或质量门槛。
