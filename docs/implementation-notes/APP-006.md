# APP-006 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/apps/applications/serializers.py、backend/apps/applications/views.py、docs/implementation-notes/APP-006.md
- 已实现行为：
  - 详情接口支持 PATCH 部分更新，仅写入请求中提交的可编辑字段，未提交字段保持不变。
  - application_status 使用既有五状态 ChoiceField 校验；公司名和岗位名继续执行去首尾空格后非空校验。
  - application_time 及六个阶段时间字段接受带时区 ISO 8601 或 JSON null；null 清空字段，空字符串返回字段级错误。
  - id、user、current_stage、created_at、updated_at 保持只读，归属仍由当前用户 QuerySet 决定。
  - 跨用户或不存在记录的 PATCH 返回 404，不会修改数据；未认证 PATCH 返回 401。
  - 成功更新返回 HTTP 200 的完整记录，current_stage 由模型属性重新派生，updated_at 由 Django auto_now 维护。
- 数据库迁移：无模型或数据库变更；makemigrations --check --dry-run 无变更。
- 配置变化：无；继续使用 PostgreSQL 配置和 JWT 认证边界。
- 已知限制：本任务不实现删除、批量编辑、乐观锁或前端编辑组件。
- 自检结果：
  - Python 3.11.13，uv 0.7.21。
  - PostgreSQL 17.11（127.0.0.1:5432）连接成功；使用会话 DATABASE_URL=postgresql://postgres:<password>@127.0.0.1:5432/postgres。
  - uv sync --frozen --group test：通过。
  - python manage.py check：通过。
  - python manage.py makemigrations --check --dry-run：通过，无变更。
  - python manage.py migrate --plan：通过，无待执行迁移。
  - APP-006 自测：15 passed。
  - 既有回归（APP-001/002/003/004、AUTH-001/002/003/004、DB-001、健康、URL）：78 passed。
  - APP-005 的 4 个已知只读测试断言冲突仍按上一任务说明记录，未因 APP-006 变化。
- 建议复测命令：在 backend 目录执行 uv run --no-sync pytest tests/test_app_006.py --tb=short -q。
- 测试完整性声明：未修改测试、断言或质量门槛。
