# APP-007 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/apps/applications/views.py、docs/implementation-notes/APP-007.md
- 已实现行为：
  - 详情视图支持 DELETE，要求 Bearer Access Token。
  - 当前用户 QuerySet 严格隔离；本人记录执行硬删除并返回 HTTP 204，响应体为空。
  - 跨用户、不存在 ID 和重复删除均返回 HTTP 404 与 {"code":"NOT_FOUND"}，不泄露记录是否存在。
  - 未认证请求由认证边界返回 HTTP 401。
  - 未引入 deleted_at 或软删除，模型和迁移保持不变。
- 数据库迁移：无模型或数据库变更；makemigrations --check --dry-run 无变更。
- 配置变化：无；继续使用 PostgreSQL 配置和 JWT 认证边界。
- 已知限制：本任务不实现回收站、软删除、批量删除或前端确认对话框。
- 自检结果：
  - Python 3.11.13，uv 0.7.21。
  - PostgreSQL 17.11（127.0.0.1:5432）连接成功；使用会话 DATABASE_URL=postgresql://postgres:<password>@127.0.0.1:5432/postgres。
  - uv sync --frozen --group test：通过。
  - python manage.py check：通过。
  - python manage.py makemigrations --check --dry-run：通过，无变更。
  - python manage.py migrate --plan：通过，无待执行迁移。
  - APP-007 自测：4 passed。
  - 既有回归（APP-001/002/003/004/006、AUTH-001/002/003/004、DB-001、健康、URL）：93 passed。
  - APP-005 的已知只读测试断言冲突未纳入本次回归，未修改其测试或生产语义。
- 建议复测命令：在 backend 目录执行 uv run --no-sync pytest tests/test_app_007.py --tb=short -q。
- 测试完整性声明：未修改测试、断言或质量门槛。
