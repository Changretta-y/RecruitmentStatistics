# SEC-001 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/config/settings.py、backend/config/middleware.py、backend/config/exceptions.py、backend/apps/health/apps.py、backend/apps/health/checks.py、frontend/src（未新增安全业务逻辑）、docs/implementation-notes/SEC-001.md
- 已实现行为：
  - `DJANGO_SECRET_KEY`/`SECRET_KEY`、`DATABASE_URL`、`DJANGO_DEBUG`、`DJANGO_ENV` 和 `CORS_ALLOWED_ORIGINS` 均从环境变量读取；不再提供危险 SECRET_KEY 默认值，也不回退 SQLite。
  - 注册 Django security system check：缺少 SECRET_KEY 或 DATABASE_URL 失败；部署检查明确拒绝 `DJANGO_DEBUG=true`，并以 `security.E102` 报告；通配 CORS 以 `security.E103` 报告并失败。
  - 生产模式默认启用 HTTPS 重定向、HSTS、HSTS preload、安全 Session/CSRF Cookie、X-Frame-Options、X-Content-Type-Options 和严格 Referrer-Policy；CORS 仅反射显式配置的来源，绝不反射 `*`。
  - 增加请求审计日志，记录 method、path、status、耗时、user_id 和 record_id；不记录请求体、Authorization、查询凭据或完整 Token。
  - 增加 DRF 统一安全异常处理；未知异常返回固定 `INTERNAL_ERROR`，敏感字段和 traceback/SQL 类内容不会回显。
  - 认证限流保持可选入口，不启用会改变现有测试确定性的全局限流策略。
- 数据库迁移：不涉及；使用现有 PostgreSQL 配置。
- 配置变化：新增上述环境变量解析及安全中间件/部署检查；未新增依赖。
- 已知限制：后端全量 pytest 在持续运行约 1 分钟后仍未结束，已按要求中止，不能将其报告为通过；SEC-001 专项测试已独立完成。
- 自检结果：
  - `uv sync --frozen --group test`：通过；恢复 pytest/pytest-django 测试组。
  - `uv run --no-sync pytest tests/test_sec_001.py --tb=no -q`：6 passed。
  - 安全环境 `manage.py check`：通过。
  - 生产安全环境 `manage.py check --deploy --fail-level WARNING`：通过。
  - `DJANGO_DEBUG=true` + `CORS_ALLOWED_ORIGINS=*`：部署检查以退出码 1 拒绝，并输出 `security.E102`、`security.E103`。
  - 后端全量 pytest：未完成；命令已中止，未伪造结果。
  - nvm Node.js v20.19.0 下前端全量 Vitest：59 passed；`npm run build`：通过。
- 建议复测命令：在 backend 目录设置 PostgreSQL `DATABASE_URL`、安全 `DJANGO_SECRET_KEY` 和显式 `CORS_ALLOWED_ORIGINS` 后执行 `uv sync --frozen --group test`、`uv run pytest tests/test_sec_001.py --tb=no -q`、`uv run python manage.py check --deploy --fail-level WARNING`。
- 测试完整性声明：未修改 backend/tests、frontend/tests、测试报告、任务单或验收文档。

