# APP-004 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/apps/applications/pagination.py、docs/implementation-notes/APP-004.md
- 已实现行为：
  - 默认 page=1、page_size=20。
  - page_size 仅接受 10、20、50、100；缺失、非法、超过 100 或不在允许集合中的值返回 HTTP 400，响应为 code=VALIDATION_ERROR，并在 details.page_size 中给出错误信息。
  - page 必须为大于等于 1 的整数；非法页码返回结构化 VALIDATION_ERROR。
  - 超出总页数返回 HTTP 200 空 results，并保留 count、page、page_size、total_pages 等分页字段。
  - next 和 previous 使用 DRF 标准链接生成，保留请求中的 page_size 及其他 query 参数。
  - 继续沿用 APP-003 的当前用户隔离和 updated_at DESC、id DESC 默认排序。
- 数据库迁移：无模型或数据库变更；makemigrations --check --dry-run 无变更。
- 配置变化：无；继续使用 PostgreSQL 配置和 JWT 认证边界。
- 已知限制：本任务不扩展搜索、筛选、排序或其他分页参数。
- 自检结果：
  - Python 3.11.13，uv 0.7.21。
  - PostgreSQL 17.11（127.0.0.1:5432）连接成功；使用会话 DATABASE_URL=postgresql://postgres:<password>@127.0.0.1:5432/postgres。
  - uv sync --frozen --group test：通过。
  - python manage.py check：通过。
  - python manage.py makemigrations --check --dry-run：通过，无变更。
  - python manage.py migrate --plan：通过，无待执行迁移。
  - APP-004 自测：13 passed。
  - 既有后端回归（APP-001/002/003、AUTH-001/002/003/004、DB-001、健康、URL）：65 passed。
- 建议复测命令：在 backend 目录执行 uv run --no-sync pytest tests/test_app_004.py --tb=short -q。
- 测试完整性声明：未修改测试、断言或质量门槛。
