# DOC-001 实现说明

- 状态：READY_FOR_TEST
- 修改范围：backend/config/settings.py、backend/config/urls.py、backend/config/schema.py、backend/apps/accounts/views.py、backend/apps/applications/views.py、backend/pyproject.toml、backend/uv.lock、README.md、docs/implementation-notes/DOC-001.md
- 已实现行为：
  - 使用 uv 添加并锁定运行时依赖 `drf-spectacular`。
  - 提供 `/api/schema/` 公开 OpenAPI schema 入口、`/api/schema/swagger/` Swagger UI 入口和 `uv run python manage.py spectacular --file <path> --validate` 生成/校验命令。
  - schema 覆盖注册、登录、refresh、logout、me、投递列表/创建、详情 GET/PATCH/DELETE；投递列表声明 search、application_status、stage、时间范围、ordering、page/page_size 参数。
  - schema 描述实际请求/响应 serializer、201/200/204/400/401/404/409 等状态码、统一 code/details 错误结构，以及 JWT Bearer security scheme。
  - README 补充 uv sync --frozen、uv run、PostgreSQL 环境变量、迁移、部署检查、schema、nvm use、npm ci、前端 test/build 和 E2E 运行说明。
  - README 不包含真实密钥、密码、Token、数据库凭据或生产地址；E2E runner 未配置时明确说明由测试任务提供。
- 数据库迁移：不涉及；schema 读取现有模型和接口，验证环境使用 PostgreSQL 配置。
- 配置变化：启用 drf-spectacular 与 AutoSchema，追加显式 BearerAuth scheme；未改变业务 API 行为。
- 已知限制：schema 入口当前公开 JSON/YAML 文档和 Swagger UI，不在本任务引入 ReDoc 或生产云资源。
- 自检结果：
  - `uv add drf-spectacular`：通过，依赖锁定为 drf-spectacular 0.30.0 及其传递依赖。
  - `uv sync --frozen --group test`：通过。
  - `uv run python manage.py check`：通过。
  - `uv run python manage.py spectacular --file <temporary-openapi.json> --validate`：通过。
  - DOC-001：4 passed。
  - README 命令和敏感信息黑盒检查：通过。
  - 前端本任务未修改 source；Node/npm 版本事实继续由 `.nvmrc`、package.json 和 package-lock.json 管理。
- 建议复测命令：在 backend 目录设置本地未提交的 PostgreSQL/Django 环境变量后执行 `uv sync --frozen --group test`、`uv run pytest tests/docs/test_doc_001.py --tb=no -q`、`uv run python manage.py spectacular --file openapi.json --validate`。
- 测试完整性声明：未修改 backend/tests、frontend/tests、测试报告、任务单或验收文档。

